import java.util.HashMap;

public class MimeTypes {
    public static HashMap<String, String> mimeTypes = new HashMap<>();
    /*
    Should we hard-initiate the hashmap with the mimetypes
    or should be read off the "mime.types" file and create the Hashmap?
     */
}